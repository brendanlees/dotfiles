# ------------------------------------------
# .aliases/gum
# ------------------------------------------

# gum filter — preserve ANSI colors from input (e.g. sesh icons)
export GUM_FILTER_STRIP_ANSI=false

# gum filter — Tokyo Night (night) palette
export GUM_FILTER_INDICATOR_FOREGROUND="#bb9af7"          # magenta — selection dot
export GUM_FILTER_SELECTED_PREFIX_FOREGROUND="#bb9af7"    # magenta — selected item prefix
export GUM_FILTER_UNSELECTED_PREFIX_FOREGROUND="#414868"  # dark gray — unselected prefix
export GUM_FILTER_MATCH_FOREGROUND="#7aa2f7"              # blue — matched characters
export GUM_FILTER_PROMPT_FOREGROUND="#73daca"             # magenta — prompt (⚡)
export GUM_FILTER_CURSOR_TEXT_FOREGROUND="#7dcfff"        # cyan — highlighted line text
export GUM_FILTER_TEXT_FOREGROUND="#a9b1d6"               # fg — list text
export GUM_FILTER_PLACEHOLDER_FOREGROUND="#414868"        # dark gray — placeholder
export GUM_FILTER_HEADER_FOREGROUND="#73daca"             # green — header (if used)
