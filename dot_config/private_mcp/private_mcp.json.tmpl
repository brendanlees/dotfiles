{{- if and (hasKey . "bw_ha_mcp") (hasKey .bw_ha_mcp "secret_path") .bw_ha_mcp.secret_path }}
{
  "mcpServers": {
    "home-assistant": {
      "type": "http",
      "url": {{ .bw_ha_mcp.secret_path | toJson }}
    }
  }
}
{{- end }}
