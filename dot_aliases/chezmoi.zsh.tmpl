# ------------------------------------------
# .aliases/chezmoi
# ------------------------------------------

# alias cz="chezmoi"
alias czdir="cd ~/.local/share/chezmoi"
alias czr="chezmoi purge --force && chezmoi init --apply brendanlees"
