# Agent workflow

Use Pi's built-in `Agent` tool for every multi-file task. Pi project system prompts live in `.pi/system.md`. Run `pi --continue-session` to resume work. The repository uses Bun; run `bun test` before completion.
