# Shared repository rules

Use Bun and run focused tests.

# Pi runtime

For Pi only, append a reminder that read-only audits must precede prompt edits. Put this system-level direction into every harness prompt.
