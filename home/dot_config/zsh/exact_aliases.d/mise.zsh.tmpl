# ------------------------------------------
# .aliases/mise
# ------------------------------------------

# Upgrade the active prompt separately from `mise upgrade --exclude starship`.
# Run this, then exec zsh to reload the replaced prompt binary.
mise-upgrade-starship() {
    "$HOME/.local/bin/mise" upgrade starship || return $?
    printf '\nstarship upgraded. run `exec zsh` to reload your shell.\n'
}
