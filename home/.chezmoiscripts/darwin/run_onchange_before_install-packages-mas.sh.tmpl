#!/bin/sh
# managed by chezmoi - re-runs when packages-darwin.yml changes

{{- $apps := fromJson (includeTemplate ".chezmoitemplates/merge-by-role.tmpl" (list .mas .)) }}

{{- if $apps }}
{{-   range $apps }}
mas install {{ .id }}
{{-   end }}
{{- end }}
